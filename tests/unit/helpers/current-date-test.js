import { currentDate } from '../../../helpers/current-date';
import { module, test } from 'qunit';

module('Unit | Helper | current date');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = currentDate(42);
  assert.ok(result);
});
